# Base URL of the backend. Leave empty to use the Vite dev proxy (/api -> http://localhost:5000).
# Set this when serving the built app from somewhere other than the Vite dev server.
VITE_API_BASE=http://localhost:5000

# Poll interval in milliseconds
VITE_POLL_MS=2000
